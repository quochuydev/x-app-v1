import { NextResponse } from 'next/server';

export async function PUT() {
  return NextResponse.json({ message: 'Todo API endpoints have been removed' });
}

export async function DELETE() {
  return NextResponse.json({ message: 'Todo API endpoints have been removed' });
}
# Database URL for PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Optional: External database URL (overrides DATABASE_URL if set)
DATABASE_URL_EXTERNAL=

# Optional: Secret key for demo purposes
SECRET_KEY=

# Optional: HCP (HashiCorp Cloud Platform) configuration for secrets management
HCP_ORG=
HCP_PROJECT=
HCP_API_KEY=